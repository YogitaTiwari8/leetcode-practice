// LeetCode: 258
// Problem name: Add Digits
// https://leetcode.com/problems/add-digits/
// Category: Math, Digital Root
// Approach:
// - **Approach 1 (O(1) Math Formula)**: Use the digital root property.
//   - If num == 0 → return 0
//   - Else → return ((num - 1) % 9) + 1
// - **Approach 2 (Iterative)**: Keep summing digits until the number is a single digit.
//   - Use a helper method `add` to calculate the sum of digits.

Solution
--
class Solution {
    // O(1) Math Formula approach (Digital Root)
    public int addDigits(int num) {
        if (num == 0) {
            return 0;
        }
        return ((num - 1) % 9) + 1;
    }

    // Iterative approach
    public int addDigitsApproch1(int num) {
        while (num > 9) {
            num = add(num);
        }
        return num;
    }

    // Helper method to sum digits
    public int add(int num) {
        int sum = 0;
        while (num != 0) {
            int rem = num % 10;
            sum += rem;
            num /= 10;
        }
        return sum;
    }
}
